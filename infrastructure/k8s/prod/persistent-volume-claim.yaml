apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: devsu-demo-sqlite-pvc
  namespace: devsu-demo-prod
  labels:
    app: devsu-demo
    environment: production
    component: database
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: "" 